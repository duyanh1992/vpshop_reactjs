import React, { Component } from 'react';
import Menu from '../../../components/MainSite/common/Menu';

export default class MenuContainer extends Component {
    render() {
        return (
            <Menu />
        )
    }
}
